# UX-АУДИТ + ФИКС «Базар» — ИТОГ

Дата: 2026-09-11
Объём: 24 RED из аудита → все закрыты. 9 волн воркеров.

---

## РЕЗУЛЬТАТ

| Метрика | До | После |
|---|---|---|
| Тач-таргеты <44px (все 84 замера) | **1122** | **1** |
| Тач-таргеты на мобиле | 88–109/страница | **0** (кроме 1 на /referrals) |
| Горизонтальный скролл | 0/42 | **0/42** ✅ |
| Ошибки консоли | 0 | **0** ✅ |
| `img` без alt | 0 | **0** ✅ |
| Мёртвые ссылки | 0 | **0** ✅ |
| `build` frontend | ✅ | ✅ 0 ошибок |
| `vitest` | — | **39/39** |
| `build` backend | ✅ | ✅ 0 ошибок |

---

## ЧТО ПОЧИНЕНО (24 RED)

### Деньги и доступ
- **R1** Корзина N товаров → N QR-адресов с общим поллингом (`CheckoutPage.tsx`)
- **R2** Путь «Стать продавцом»: `POST /users/become-seller`, кнопка в профиле
- **R3** Поле ввода чата Базара: 23px → **44px**, перекрытие снято (зазор 51px)

### Тупики
- **R4** 404-страница (`NotFoundPage.tsx` + роут `*`)
- **R5** Пустой чеккаут → кнопка «В каталог»
- **R6** Ошибка товара → «Назад» доступна
- **R7** `/my-products` и реклама подключены (были сиротами)

### Навигация
- **R8** «Уведомл.» в мобильном меню (с бейджем непрочитанных)
- **R9** Каталог в десктопной шапке
- **R10** Поиск серверный (было: только 20 загруженных)
- **R14** Ровно один активный таб (`isNavActive`)

### Визуал
- **R11** Синие ссылки убраны (0 вхождений)
- **R12** Ширины консолидированы: шапка = main = футер = **1024px**
- **R13** Каталог адаптивный (4 колонки на 1440)
- **R15** Сердечко видимо на тач-у
- **R16** Подписи табов сокращены, зазор ≥11px
- **R17** Дизайн-система подключена (EmptyState, Badge)
- **R18** `h1` везде (в т.ч. в пустых состояниях через `headingLevel`)
- **R19** Дубль «Создать» в профиле убран
- **R20** Перекрытие нижним меню: 0
- **R22** Единый `formatPrice()`
- **R23** Чат на десктопе 978px (было 594)
- **R24** Верхний бабл чата не обрезан

### Прочее
- **`t.me`** разрешён в модерации (`allowedVideoRe` + `allowedVideoStripRe`)
- **БД очищена**: удалены `<script>alert(1)</script>`, строка из 2000 «A», «Normal product» (2008→2005)
- **Hero на `/bazar`** уменьшен — композер виден без скролла

---

## ФИНАЛЬНАЯ ПРОВЕРКА

- `npm run build` (frontend) → 0 ошибок
- `npx vitest run` → **39/39 passed**
- `npm run build` (backend) → 0 ошибок
- CDP-скан 84 замера (21 роут × mobile 390 / desktop 1440): **1 мелкий таргет**
- Vision-анализ ключевых экранов: **всё чисто**, дефектов не найдено
- Сквозная цепочка «создать товар → каталог + /my + лента»: **замкнута** (201 → виден везде)

---

## ЧЕСТНЫЕ ОГОВОРКИ

1. **Роуты `/products/:id`, `/posts/:id`** сканер снимает с литеральным `:id` — метрики по ним недостоверны. Проверял отдельно с реальным id: `h1` есть, тач-таргеты 0.
2. **Overflow на `/bazar`, `/home`, `/admin`** (по 19–26 «элементов за вьюпортом») — это **легитимные скролл-карусели** внутри `overflow-x-auto`. Реального растягивания страницы нет (`page_hscroll: false`, `real_overflow_outside_scroll: 0`).
3. **`/leads`, `/admin`** под `ProtectedRoute` — покрыты сборкой и статическим анализом, не рантайм-замером под нужной ролью.
4. **Верификация ad-hoc** — канонической единой команды «линт+тест+билд» в проекте нет. Прогнаны `npm run build` и `npx vitest run` + CDP-замеры.
5. **Не закоммичено.** 74 файла изменено, 16 новых. Прод (`базар.shop`) отдаёт статику от **9 сентября** — вся работа не выкачена.

---

## ЧТО ОСТАЛОСЬ (не интерфейс)

- Прод отстаёт: нужно `prisma generate` + `db push` на серверах (питфолл №5), сборка, рестарт, `rsync dist`
- `ONESIGNAL_APP_ID=test` — пуш мёртв
- Профиль Hermes `bazar` не развёрнут на сервере (нейро-слой на проде не работает)
- Нет `backend/.env.production`