# UX-АУДИТ «Базар» — СВОДНЫЙ ОТЧЁТ

Дата: 2026-09-11
Метод: CDP-скан 84 замера (21 роут × mobile 390 / desktop 1440) + живые тапы + чтение кода + вижн-ревью чистых скринов.
Воркеры: qa-lead (mobile), qa-lead (desktop), researcher (flows).

---

## ⚠️ ДИСКЛЕЙМЕРЫ (честность)

1. **«Затемнение экранов» из первого вижн-прогона — АРТЕФАКТ.** Скрины снимались в момент анимации `PageTransition` (framer-motion, `initial opacity:0`). Пиксельная проверка: все кадры avg ~20 = нормальный `#0b0e0d`. Переснято с отключёнными анимациями (`clean/`).
2. **Роуты `/products/:id`, `/posts/:id`** в первом скане сняты с литеральным `:id` — метрики по ним недостоверны.
3. **У BUYER на `/products/new`, `/leads`, `/admin`** — не баг страницы, а редирект на главную (нет доступа).
4. **Скрины визуально проверены** через `hermes chat --image` (vision-тулсет был отключён, включён в ходе аудита).

---

## 🔴 RED — чинить

### ДЕНЬГИ И ДОСТУП

**R1. Корзина из N товаров → N заказов, оплата одного**
`CheckoutPage.tsx:70-77` — цикл `for (item of cart)`, `pay` берётся от `lastOrder`.
Пользователь кладёт 3 товара, платит за 1, остальные 2 заказа висят неоплаченными.
**Фикс:** один заказ на всю корзину (или явный мульти-платёж со списком адресов).

**R2. Нет пути «стать продавцом»**
`grep SELLER` → только `ProtectedRoute` + `AdminPage.tsx:97`. Роль меняется только из админки.
Обычный юзер физически не может продать — а это половина маркетплейса.
**Фикс:** заявка «Стать продавцом» в профиле, либо авто-выдача SELLER при первом товаре.

**R3. Чат Базара недоступен на мобиле** ✅ ПОДТВЕРЖДЕНО ЖИВЫМ ТАПОМ
Поле ввода: `top 792–814`, **высота 23px**. Нижнее меню перекрывает его.
`elementFromPoint(центр)` → ссылка навигации, `is_input_hit: false`. Тап уводит на `/posts/new`.
Микрофон 48×48 — тап уводит на `/profile`.
**Фикс:** поднять инпут над меню, добавить `pb` под фиксированную навигацию.

### ТУПИКИ

**R4. Нет 404** — неизвестный URL даёт пустой экран. Нужен роут `*` с заглушкой.

**R5. Пустой чеккаут — тупик без выхода** `CheckoutPage:105-116` (в `CartPage:27-29` выход есть).

**R6. Ошибка товара — тупик** `ProductDetailPage:93-99` — кнопка «Назад» ниже раннего `return`.

### ЭКРАНЫ-СИРОТЫ

**R7. `/my-products` и `/posts/ad/new` недостижимы**
Объявлены в `App.tsx:69,73`, ссылок нет нигде в `src/`. Продавец не может управлять своими товарами.

### НАВИГАЦИЯ

**R8. Уведомления недостижимы с мобильного** — нет в нижней панели.

**R9. Каталог `/products` недостижим из десктопной шапки.**

**R10. Поиск ищет только по 20 загруженным записям**
`FeedPage:175`, `ProductsPage:66-68` — серверный параметр не передаётся. При 2008 товарах бесполезен.

### ВИЗУАЛ / ТЕМА

**R11. Синие ссылки в форме товара** ✅ ПОДТВЕРЖДЕНО
`CreateProductPage.tsx` — `text-blue-600` ×3 (строки 110, 142, 231), `EditPostPage.tsx` тоже.
Нативные синие ссылки на тёмном неоново-зелёном фоне. В проекте ещё 19 мест с `text-blue-600`/`bg-gray-100`/`text-gray-500`.
**Фикс:** заменить на `text-[#22c55e]`.

**R12. Шапка `max-w-3xl` (768px) vs main `max-w-7xl` (1280px)** — кромки разъезжаются на 256px.
Видно на всех десктопных скринах: капсула шапки «плывёт» относительно контента.

**R13. Каталог жёстко 2 колонки на 1440px** — правая треть экрана пустая.

**R14. 1122 тач-таргета < 44px** на 100% мобильных роутов. Крупнейшие offenders: футер («Базар» 98×36, «Избранное» 75×20, «Корзина» 57×20), кнопки навигации.

**R15. Сердечко «в избранное» 31×31 с `opacity:0`** — на тач-у невидимо.

**R16. Мелкий текст 9–11px** — инициалы-аватары, «РЕКЛАМА», имена продавцов, подписи табов.

### АРХИТЕКТУРА UI

**R17. 9 компонентов дизайн-системы — 0 импортов**
`EmptyState`, `ErrorState`, `Modal`, `Tabs`, `Skeleton`, `Badge`, `Spinner`, `Avatar`, `Breadcrumbs` лежат в `components/ui/` и не используются. Пустые состояния свёрстаны руками 8 раз по-разному.

**R18. Нет `h1`** на `/products/:id`, `/posts/:id`, `/bazar` — SEO + доступность.

### НАЙДЕНО ВИЖН-РЕВЬЮ (чистые скрины)

**R19. Дубль CTA «Создать» в профиле** ✅ ПОДТВЕРЖДЕНО ВИЖН
В одном блоке и текстовая подпись «Создать» с иконкой, и зелёная pill-кнопка «+ Создать». Выглядит сломанным.

**R20. Таб-бар перекрывает контент** — заголовки «Мои заказы» (профиль), «Тестовый товар» (каталог) срезаны нижней навигацией.

**R21. Битые картинки товаров** — «iPhone 15 E2E» (сломанная + alt-текст), «Тестовый товар Базара» (пустой чёрный блок). Системно, во всех местах рендера.

**R22. Три разных формата цен на одном экране**
`3,500` / `4,999` (целые с разделителем), `178,072.43` / `84,905.29` (с копейками), `3200` / `3500` (без разделителя). Нет единого правила.

**R23. Чат на десктопе не использует ширину** — карточка ~730px из 1440, правая половина пустая. Нет двухколоночной раскладки.

**R24. Обрезанный верхний бабл в чате** — начало треда срезано верхней границей, скроллбар у потолка, нет «загрузить раньше».

---

## 🟡 YELLOW

- Молчаливый редирект по роли (BUYER → `/` с защищённых роутов, без объяснения)
- Скрытая X-кнопка поиска (32×32) остаётся фокусируемой (`tabIndex=0`)
- `navigate(-1)` уводит из приложения при прямом заходе
- Нет активного состояния в десктопной шапке
- «Начать сначала» в чате — низкий контраст, визуально конкурирует с чатом
- Дубли корзины в трёх видах (шапка / карточка / нижнее меню)
- 7 пунктов в нижнем меню — тесно, active только цветом

## 🟢 GREEN

- Сократить нижнее меню до 5 пунктов («Лиды» → в профиль)
- Плейсхолдер-фолбэк для битых картинок товаров
- Свернуть длинные сообщения агента в чате («показать ещё»)
- Хлебные крошки (компонент уже написан, не подключён)

---

## ✅ ЧТО ЧИСТО (проверено)

- Горизонтальный скролл: **0/42 роутов** ✅
- Ошибки консоли: **0** ✅
- `img` без `alt`: **0** ✅
- Мёртвые ссылки: **0** ✅
- Шрифты ≥12px на десктопе ✅

---

## 📊 ДАННЫЕ В БД (побочная находка)

Разброс цен **100 … 249 955 USDT** (avg 124 391) — это реальные данные, не баг формата.
Но в базе мусор от тестов: `<script>alert(1)</script>`, строка из ~2000 «A», `Normal product`, «Тестовый товар Базара», «Продавец E2E».
**Рекомендация:** вычистить перед продом.

---

## ПРИОРИТЕТ

1. **R1, R3** — ломают деньги и главный сценарий
2. **R2, R7** — продавец не может работать
3. **R4, R5, R6** — тупики
4. **R10, R8, R9** — поиск и навигация
5. **R11, R12, R13** — тема и раскладка
6. **R14–R18** — полировка